# Contact form (optional). If unset, the contact form falls back to a mailto: link.
# Create an API key at https://resend.com and verify a sending domain.
RESEND_API_KEY=
# Where contact form submissions are delivered.
CONTACT_TO_EMAIL=mnawawra900@gmail.com
# The verified "from" address for Resend (e.g. "Portfolio <hello@yourdomain.com>").
CONTACT_FROM_EMAIL=Portfolio <onboarding@resend.dev>

# Public site URL (used for canonical, sitemap, OG). No trailing slash.
NEXT_PUBLIC_SITE_URL=https://mohammadalnawawreh.com
